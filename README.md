# api-first-video-app
API-first video application built with Flask, MongoDB, and React. The frontend acts as a thin client and depends entirely on backend APIs for authentication and video streaming.
